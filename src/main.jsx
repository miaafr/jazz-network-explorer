import React from "react";
import ReactDOM from "react-dom/client";
import App from "./jazz-network-explorer";




ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
